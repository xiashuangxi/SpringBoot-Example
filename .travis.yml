language: java
install: skip

os: linux
dist: trusty
jdk: oraclejdk8

branches:
  only:
    - main
cache:
  directories:
  - $HOME/.m2
  - $HOME/.gradle

script: 
  # - chmod 777 ./test/run.sh
  # - test/run.sh
  - cd ./springboot-main-slave 
  - pwd
  - ls -la
  - chmod 777 gradlew
  - ./gradlew build --scan -s